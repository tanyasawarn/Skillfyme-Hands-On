# Phase 3 — Stage Checklist

Combined Dev A + Dev B implementation list for Phase 3 (Projects + T3 Cloud Sandboxes).
Source of truth: `PLAN_PHASE3_PROJECTS.md`. An item is checked **only when it is complete
end-to-end** — code written, builds clean, its own tests pass, and (where it touches a
runtime path) verified against the running stack. A verification note is added under each
completed item.

Legend: `[ ]` not started · `[~]` in progress · `[x]` done (with note) · `[B]` blocked on
external infra / human action.

---

## Stage 0 — Gate & Contracts

- [B] **0.1** File AWS account-quota support ticket (target: peak-T3 × 2 accounts) — day 1, weeks of lead time
      _Blocked: needs a real AWS Organizations payer account. Ops action, not code._
- [x] **0.2** Load harness in `evaluation/phase1/load/`: `load.js` (k6, deliverable of record — §13.1 thresholds encoded as `thresholds:` so the run self-grades) + `load_driver.py` (stdlib-only, same scenario/env-vars/gate, runnable now) + `check-orphans.sh` (zero-orphan gate: `increase(reaper_orphans_found_total[window]) == 0` + namespace cross-check) + `seed-load-users.sh` + `results-template.md` + `README.md`. Scenario per VU: dev-login → pick published L1 lab → create → provision → connect → **start** → 20 workspace writes ("20+ cmds") → check → submit → teardown. `LOAD_PREMINTED_TOKENS` path for the 200-distinct-user run.
      _Verified: `node --check load.js`, `python3 -m py_compile load_driver.py`, `bash -n` on all scripts — clean. `load_driver.py` run live against the running stack (compose infra + local orchestrator/practice-core): with the demo user's prereq mastery seeded, a 1-VU run is a full green pass — provision 1.0, connect 1.0, time-to-ready p95 ≈ 1.6 s, **submit 1.0 / teardown 1.0** (submit() is the teardown trigger — no separate learner destroy route; both runners updated to treat submit-success as env-released and `check-orphans.sh` as the leak backstop), 2/2 workspace writes against a real k3s pod, exit 0. Self-grades and exits non-zero on a miss (confirmed: exit 1 when prereq mastery unseeded). Surfaced — README documents + harness mitigates — three real-run prerequisites: (1) practice-core's 1-active-env-per-learner quota ⇒ need 200 distinct users, (2) per-user prerequisite-mastery seeding, (3) `dev-login` throttler ⇒ `LOAD_PREMINTED_TOKENS`. Executing at 200 VUs on a multi-node cluster is 0.4 (blocked)._
- [x] **0.3** Namespace-churn soak in `evaluation/phase1/soak/`: `namespace-churn-soak.sh` (+ `results-template.md`, `README.md`). Drives `EnvironmentOrchestrator/Provision`+`/Destroy` over gRPC at `3 × SOAK_PEAK` sustained concurrency (parameterised to the target cluster's projected peak, not a hardcoded number), each env cycling every `SOAK_ENV_LIFETIME_SEC`; `SOAK_DURATION_MIN` churn then `SOAK_HOLD_MIN` hold. `SOAK_TIER=T1|T2` (T2 path is 0.5). Gate = 4 criteria: (1) `increase(reaper_orphans_found_total)` == 0 during churn **and** the hold, (2) every provisioned env explicitly destroyed (0 failed Destroy), (3) reaper TTL backstop ≤ 5 % of teardowns, (4) `env-*` namespace count returns to baseline. Exits non-zero on any failure.
      _Verified: `bash -n` clean. Ran live at `SOAK_PEAK=2` (×3 = 6-way) against the local orchestrator — the run **found a real bug in an early draft**: synthetic string `attempt_id`s (`soak-w1-i1-…`) were accepted by `Provision` but `env.environment.attempt_id` is a `uuid` column, so every `Destroy` ownership check returned `PermissionDenied` and the gate correctly FAILed criteria 1+2 (teardown gap). Fixed: workers now mint real UUIDs (`uuidgen` / `uuid.uuid4()`); re-verified Provision→Destroy round-trips cleanly (`{}`). Also confirmed: worker pool spawns, metric-scrape + namespace cross-check + 4-criteria gate all execute and the script exits non-zero on failure; preflight now warns loudly if `SOAK_ORCH_METRICS_URL` isn't serving `orchestrator_reaper_orphans_found_total` (the local `go run` orchestrator doesn't expose its metrics port on the host). Full 3×-peak run on a multi-node cluster is 0.4/0.5 (blocked)._
- [B] **0.4** Execute 0.2 + 0.3 on a real multi-node cluster; commit `loadtest-<date>.md` with `reaper_orphans_found_total == 0` sustained
      _Blocked: needs a real multi-node cluster (Docker-Desktop k3s can't hold 200 concurrent pods)._
- [B] **0.5** Run the same soak against the T2 teardown path (no microVM / CNI leak)
      _Blocked: same as 0.4._
- [B] **0.6** Reviewer sign-off on `PHASE1_MVP_COMPLETION.md` §8 + new `PHASE2_TEARDOWN_SOAK.md` — HARD GATE: no T3 compute build (Stage 2/3/4) until this is signed
      _Blocked: needs 0.4 + 0.5 results and a human reviewer._
- [x] **0.7** `contracts/orchestrator.proto`: `SnapshotManifest` message (tf backend/serial/workspace, k8s inventory URI, cloud inventory URI + count, sandbox account id, captured_at) + `SnapshotResponse.manifest` (T3-only, additive) + `RestoreRequest.cloud_account_hint`. Go stubs regenerated. CHANGELOG entry (MINOR).
      _Verified: `buf lint` clean; `buf breaking` vs `origin/main` clean (additive only); `buf generate` regenerated `orchestrator/pkg/pb/*.pb.go` and re-running it is idempotent (stubs fresh); `cd orchestrator && go build/vet/test ./...` all clean, `gofmt` clean; practice-core proto loads fine (grpc tests 12/12, unit suite 170/170). `Snapshot`/`Restore` still return `Unimplemented` — implementing them is Stage 3.3; this only widens the contract._
- [x] **0.8** `contracts/events.*`: taxonomy entries added — `MILESTONE_GATED`, `DEFENCE_MESSAGE` (Project), `ACCOUNT_CLAIMED` / `ACCOUNT_NUKED` / `ACCOUNT_QUARANTINED` (Cloud account); `MILESTONE_SUBMITTED` Notes filled in (was already reserved). 5 new `contracts/events/*.schema.json` payload schemas. TS `ATTEMPT_EVENT_TYPES` updated in lockstep. CHANGELOG entry (MINOR).
      _Verified: all 5 new schema files parse as valid JSON with `type: object` + `required[]`; `npx tsc --noEmit` clean; new `attempt-event-type.spec.ts` 3/3; full practice-core unit suite 170/170; `eslint.boundaries` clean; `orchestrator/` `go build ./...` clean. `ReplayService` switch unaffected (no-op `default:`)._
- [x] **0.9** `contracts/activity_spec.schema.json`: `mode: PROJECT` support — `milestones[]`, `defence`, `environment.cloud` (`regions` + `sku_exceptions`), `tasks[].validators[].config` per-type config for the 6 T3 validator types. K10's `ActivitySpec` TS mirror updated in lockstep (`ActivitySpecMilestone` / `ActivitySpecDefence` / `ActivitySpecEnvironmentCloud` / `ActivitySpecValidatorConfig` + 6 per-type interfaces). CHANGELOG entry added (MINOR — additive, no `required` change).
      _Verified: `contracts/activity_spec.schema.json` parses as valid JSON; `npx tsc --noEmit` clean; `activity-spec.spec.ts` (added a full PROJECT-mode spec test + a milestone-enum negative control) + `spec-lint.service.spec.ts` = 10/10; full practice-core unit suite 167/167. Existing GUIDED_LAB / PRODUCTION_SIM content unaffected. No proto change._
- [x] **0.10** Schema migration `practice-core/db/migrations/0010_project_mode.sql` (`attempt` schema, Dev B): `attempt.project_milestone_state` (PK `(attempt_id, milestone_key)`; `milestone_key` CHECK-enum design/infra/implementation/hardening/final; `status` CHECK-enum LOCKED/OPEN/SUBMITTED/GATED_PASS/GATED_FAIL; `ordinal`, `submitted_at`, `gated_at`, `score numeric(5,4)`, `rubric_level` 1–5 CHECK, `attempt_count`; 2 indexes incl. a partial `WHERE status IN ('OPEN','SUBMITTED')` instructor-view index) + `attempt.project_submission` (append-only history: `repo_ref`, `commit_sha`, `attempt_number`, per-submission `outcome`/`score` snapshot). `ProjectRepository` (Kysely, extends `BaseRepository`) — `seedMilestones` (idempotent ON CONFLICT), `listMilestones`, `getMilestone`, `markSubmitted`, `applyGateOutcome` (transactional: GATED_PASS opens exactly the next LOCKED ordinal), `recordSubmission`, `stampSubmissionOutcome`, `listSubmissions`. `schema.ts` types + `Database` interface entries; `truncateAll` updated. `project.integration.spec.ts` — 8 tests vs real Postgres.
      _Verified: fixed a real defect — `schema.ts` typed `score` as `string | null`, breaking `npx tsc --noEmit` at `applyGateOutcome` (jest/ts-jest doesn't full-typecheck so unit tests hid it); corrected to `number | null` to match the `attempt.attempt_score.final_score` numeric-write convention, and dropped two now-unnecessary `as unknown as` casts flagged by eslint. Now clean: `npx tsc --noEmit`, `eslint` on the project module + spec, `npm run lint:boundaries`, unit suite 170/170, `project.integration.spec.ts` 8/8. Migration applied to `practice_engine` + `practice_engine_test`, re-run is an idempotent no-op; live CHECK enforcement confirmed (out-of-enum `milestone_key` rejected, `rubric_level` 7 rejected)._
- [x] **0.11** Schema migration `orchestrator/db/migrations/0005_cloud_account.sql` (`env` schema, Dev A): `env.cloud_account` table — `aws_account_id` PK, `state` (AVAILABLE/IN_USE/NUKING/QUARANTINED CHECK), `region`, `attempt_id` (no cross-boundary FK), `budget_usd`, `sku_exceptions[]`, lifecycle timestamps, quarantine fields; 3 partial indexes (available-by-region, by-attempt, quarantine-queue); 2 CHECK constraints enforcing state-machine invariants (IN_USE ⇒ has attempt+budget; AVAILABLE ⇒ clean). `usage_meter.cloud_cost_usd` was **already present** in `0001_env_and_billing.sql` — no column add needed.
      _Verified: applied to the running `practice_engine` (compose Postgres) and `practice_engine_test` — `CREATE TABLE` + 3 indexes + 4 CHECKs present (`\d env.cloud_account`); re-run is a clean no-op (idempotent, matches the compose `db-migrate-orchestrator` one-shot pattern); both invariant constraints reject bad rows live (AVAILABLE-with-attempt → rejected; IN_USE-without-budget → rejected; proper claim → accepted); `orchestrator/` `go build ./...` + reaper db tests clean._

---

## Stage 1 — AWS Foundation & Project Skeleton (parallel tracks)

**Stage status: NOT COMPLETE — 4 / 9 done (`[x]`), 5 blocked (`[B]`).**
All practice-core code (1.6, 1.7, 1.8, 1.9) is complete and verified against
the running stack; the platform-services deploys with a local target (1.4
Forgejo, 1.5 ClickHouse) are really deployed and verified end-to-end. The 5
`[B]` items — 1.1/1.2/1.3 `tofu apply` and the production side of 1.4/1.5 —
need a real AWS Organizations payer account (this machine has only placeholder
`~/.aws` credentials); 1.8's CLOUD_ASSERT live PASS/FAIL and 1.9's kappa run
need real AWS creds / `ANTHROPIC_API_KEY` + SME time respectively. Stage 1 is
checked complete only when all 9 are `[x]`.

- [B] **1.1** AWS Organization + OU scaffold — `infra/aws-org/main.tf`: `aws_organizations_organization` (ALL features, SCP + TAG policy types), 3 OUs (`Platform` / `ContentCI` / `LearnerSandboxes`), org-wide multi-region CloudTrail → payer-account S3 bucket (versioned, public-access-blocked, correct bucket policy), AWS Config recorder + delivery channel + status, GuardDuty detector + `auto_enable_organization_members = ALL`. Outputs: `organization_id`, `ou_ids`, `root_id`.
      _`tofu fmt` + `tofu validate` clean (aws provider ~> 5.0). **Apply `[B]`** — creating an Organization is near-irreversible and needs real payer-account admin credentials; this machine has only placeholder `~/.aws` creds. `tofu plan` needs credentials._
- [B] **1.2** SCP framework — `infra/aws-org/scp/`: 6 JSON policy documents (`01-region-deny` templated with the two allowed regions, `02-expensive-sku-deny` with the `practice-engine:sku-exception` tag mechanism, `03-org-boundary-deny` = leave-org + security-service-tamper + delete-`PlatformNukeRole`/`LearnerSandboxRole`, `04-iam-hardening-deny` = no IAM users / no long-lived keys, `05-public-sharing-deny` = public S3/AMI/snapshot, `06-mail-abuse-deny` = SES + SNS-SMS with a messaging-exception tag) + `main.tf` (creates all 6 as `aws_organizations_policy`, attaches region/SKU/public/mail/IAM to the LearnerSandboxes OU and org-boundary at the root) + `verify/red-team.sh` (assumes `LearnerSandboxRole`, asserts each denied action returns `AccessDenied`, plus an allowed-region control).
      _All 6 `*.json` validated (2012-10-17, ≥1 statement each); `main.tf` `tofu fmt` + `tofu validate` clean; `red-team.sh` `bash -n` clean. **Attach + red-team run `[B]`** — needs the Organization from 1.1._
- [B] **1.3** Account-baseline module — `infra/account-baseline/main.tf`: the Terraform the Account Pool Manager (Stage 2.4) applies per freshly-claimed sandbox account — S3 backend block (partial; per-account key supplied at `tofu init`), `aws_iam_openid_connect_provider` trusting the platform IdP, `LearnerSandboxRole` (trust policy binds `sub` = `attempt_id`, `aud` = platform client id, 1h max session) + `PowerUserAccess`, `PlatformNukeRole` (assumable only by the Platform account) + `AdministratorAccess`, a `practice-require-tags` deny policy (no `ec2:RunInstances`/`CreateVolume`/`rds:CreateDBInstance`/`s3:CreateBucket`/`eks:CreateCluster` without `attempt_id`). Outputs: role ARNs + OIDC provider ARN.
      _`tofu fmt` + `tofu validate` clean. **Apply `[B]`** — needs a real claimed sandbox account + the platform IdP (Stage 2.1)._
- [x] **1.4** Per-learner Git hosting — `infra/git-hosting/`: **real self-hosted Forgejo deployed locally and verified end-to-end.** `compose/docker-compose.git-hosting.yml` (Forgejo 9 on the compose Postgres, `forgejo_data` volume, no open signup, disabled built-in SSH — HTTP+token push) + `compose/forgejo-app.ini` (config reference) + `scripts/bootstrap-admin.sh` (idempotent admin user + `write:admin,write:organization,write:repository,write:user` token) + `scripts/verify-git-hosting.sh`. Production: `helm/` (StatefulSet + PVC for permanent repo storage + Ingress; `helm lint` + `helm template` clean, renders 3 resources) + `terraform/main.tf` (RDS `db.t4g.small` for Forgejo metadata + SG + Route53 alias; `tofu fmt`/`validate` clean).
      _`verify-git-hosting.sh` passes end-to-end against the running local Forgejo: create per-learner org → create seeded repo → clone over HTTP with an admin token → push a milestone commit → read `/api/v1/.../commits` (2 commits) + `/contents/DESIGN.md` back → retention check (repo on the platform host, not a sandbox) → cleanup. **Production apply `[B]`** — needs the Platform AWS account + a cluster with an ingress controller + a StorageClass._
- [x] **1.5** ClickHouse — `infra/clickhouse/`: **real single-node ClickHouse deployed locally and verified end-to-end.** `compose/docker-compose.clickhouse.yml` (ClickHouse 24.8, profile `clickhouse`, HTTP :8123) + `compose/init/01-schema.sql` (`practice_analytics.attempt_events` MergeTree partitioned by month + `attempt_hourly` AggregatingMergeTree + the `attempt_hourly_mv` materialised view + a `_merged` convenience view — the Stage 4.1/B8 ingestion target) + `scripts/verify-clickhouse.sh`. Cloud: `cloud/main.tf` (`clickhouse_service` dev tier, `ClickHouse/clickhouse ~> 3.0` provider, idle-scaling, IP allowlist; `tofu fmt`/`validate` clean against the real provider) + `cloud/apply-schema.sh` (loads the same `01-schema.sql`).
      _`verify-clickhouse.sh` passes: schema present → insert a synthetic `MILESTONE_SUBMITTED` + `MILESTONE_GATED` pair → 2 raw rows land → the hourly rollup MV aggregated them (`events=2, milestones_gated=1`) → cleanup. **Cloud apply `[B]`** — needs a ClickHouse Cloud org + API key._
- [x] **1.6** Project-mode milestone state machine — new `practice-core/src/modules/project/` module: `ProjectService` (the state machine — `design→infra→implementation→hardening→final`, `LOCKED/OPEN/SUBMITTED/GATED_PASS/GATED_FAIL`; lazy-seed on first list; `submitMilestone` runs the milestone's validator set + rubric slice, applies the gate, opens exactly the next LOCKED milestone on a blocking `GATED_PASS`, records the submission, emits `MILESTONE_SUBMITTED` + `MILESTONE_GATED`; per-`gate` score math; env lifecycle via `PROJECT_ORCHESTRATOR` — snapshot-suspend on pass, provision on fail-to-iterate, `design` needs no env). Design hard-gate = `RUBRIC_MIN_LEVEL` on `rub.architecture.v3`'s `overall` at level 3, carrying the `provisional` flag (rubric uncalibrated). Falls back to the canonical 5-stage sequence when the spec has no `milestones[]`. `ProjectController` — `GET /v1/practice/attempts/{id}/milestones`, `POST .../milestones/{key}/submit`, `POST .../defence/messages` (guarded by `AttemptOwnershipGuard`). `DefenceService` — turn-by-turn transport + `DEFENCE_MESSAGE` events (the grounded generator + scorer is Stage 3.8). `ProjectModule` wired into `AppModule`; `FakeProjectOrchestrator` as `PROJECT_ORCHESTRATOR` until Stage 3.2. Also fixed: `DatabaseModule.onModuleDestroy` was a no-op → now drains the pg pool (`db.destroy()`); `BaseGrpcClient` gained an `onModuleDestroy` that closes the gRPC channel.
      _`npx tsc --noEmit`, `eslint`, `npm run lint:boundaries` (widened the evaluation SEAM for `ai-grader.interface` + `rubric.repository` per B5, exported both from `EvaluationModule`) all clean. `project-milestones.integration.spec.ts` — 6 tests vs real Postgres + FakeAiGrader + FakeProjectOrchestrator: lazy seed, LOCKED-submit rejection, the design rubric gate + provisional flag + cascade, resubmit-after-GATED_FAIL, validator-only milestones auto-passing to the end, and `MILESTONE_SUBMITTED`/`MILESTONE_GATED` emission. Full unit suite 183/183._
- [x] **1.7** `git.service.ts` — `practice-core/src/modules/project/`: `ForgejoClient` (typed REST client over the Forgejo API — `ensureUser`/`ensureOrg`/`ensureRepo` idempotent, `putFile` seeding primitive, `getFileContent`/`listCommits`/`getCommitDiff`, `getDiff` = concatenated per-commit diffs over base…head since Forgejo's `/compare/*.diff` 500s on this version, `createUserScopedToken` via the learner's own basic auth since an admin token is rejected there) + `GitService` (`enrolProject` = ensure user/org/repo + seed the requirements pack + add collaborator + mint a `write:repository` push token; `recordMilestoneSubmission` resolves HEAD and writes `project_submission.repo_ref` + `commit_sha`; `listCommits`/`readFile`/`diff` for the viva generator; `deleteLearnerRepo` for test cleanup only — retention means the state machine never deletes). Degrades to no-ops when `FORGEJO_BASE_URL`/`FORGEJO_ADMIN_TOKEN` are unset.
      _`git-service.integration.spec.ts` — 5 tests against the **real local Forgejo** (skips when unconfigured): `isEnabled`, `enrolProject` (repo provisioned + both seeded files readable + scoped push token returned), a **real `git push` over HTTP with the learner token** then history + `DESIGN.md` read back, `recordMilestoneSubmission` (40-hex HEAD resolved + recorded), and `diff` base…head containing the added line. `tsc` + `eslint` clean._
- [x] **1.8** Early validator executors — `practice-core/src/modules/evaluation/t3/`: `T3ValidatorExecutor` (implements the `ValidatorExecutor` seam — routes `IAC_STATE`/`CLOUD_ASSERT`/`STATIC_ANALYSIS` to typed handlers, delegates every other type verbatim to `GrpcValidatorExecutor`, so `ValidatorRunnerService` is unchanged and `ERROR` is still filtered from scoring). `ShellRunner` abstraction with two backends: `OrchestratorShellRunner` (`ExecShell` RPC via a 2nd `BaseGrpcClient` subclass, `forAttempt()` binds the ownership id — production) and `LocalShellRunner` (argv-only, cwd-confined, timeout-killed — the pre-driver de-risking path). Handlers: `runIacState` (`terraform plan -detailed-exitcode` no-drift, `terraform state pull` + local-`.tfstate` remote-backend check, secret-shaped-value scan of pulled state), `runStaticAnalysis` (tfsec/checkov/trivy JSON parsed to typed findings, severity + `max_findings` gate), `runCloudAssert` (5 read-only `aws` CLI checks — no-public-S3, RDS-encrypted, no-world-open-admin-ports, non-default-VPC, no-wildcard-admin-policy). Every platform failure → `ERROR`, never `FAIL`. Wired as the default `VALIDATOR_EXECUTOR` (opt out with `T3_VALIDATORS=off`); `T3_LOCAL_FIXTURE_DIR` selects `LocalShellRunner`. `weighted-kappa.ts` extracted + shared with 1.9.
      _`t3-validators.integration.spec.ts` — 14 tests against **real `terraform` + real `tfsec`** + a committed static fixture repo (`test/fixtures/t3-terraform/{clean,drifted,with-secrets,insecure}`, provider dirs gitignored, `terraform init` in `beforeAll`, skip when the binary is absent): IAC_STATE PASS on clean / FAIL on drift / FAIL on secret-in-state / FAIL on local-backend, ERROR (not FAIL) for a missing dir + no-checks config; STATIC_ANALYSIS FAIL on `insecure/` at NONE / PASS on `clean/` at LOW / PASS under a high `max_findings` budget, ERROR for no-tool + missing-binary; CLOUD_ASSERT ERROR for unknown check / no checks / aws-unauthenticated. `t3-validator.executor.spec.ts` — 4 dispatch tests. `tsc` + `eslint` + boundaries clean. **The one hand-made sandbox account run for CLOUD_ASSERT's live PASS/FAIL path is `[B]`** (needs real AWS creds; the ERROR path is covered)._
- [x] **1.9** `rub.architecture.v3` — `content/rubrics/rub.architecture.v3.yaml`: engineered rubric for the milestone-1 design ("appropriateness given the stated constraints, not the textbook answer" — §12.3), 5 criteria (`constraint_fit`, `failure_mode_reasoning`, `tradeoff_honesty`, `data_model_soundness`, `overall` = the level-3 hard gate), `human_review.policy: ALWAYS_PROVISIONAL_UNTIL_CALIBRATED`. Reuses the Phase-2 `ClaudeAiGrader` plumbing unchanged (its system prompt already delimits + flags injection). Calibration harness in `evaluation/phase1/rubric-calibration/`: `scripts/rubric-calibrate.ts` (loads the SME-scored case set, runs the configured `AI_GRADER`, computes per-criterion weighted Cohen's kappa + exact-match + MAE, asserts the injection-defence case scores `overall ≤ 2` with a prompt-injection flag, exits non-zero unless `overall` kappa ≥ 0.6 **and** the injection assertion holds; `SKIP` under FakeAiGrader). 6 hand-built cases spanning the range + 2 deterministic constraint sets + `rub-calibration.md` (the record: threshold, run table, sign-off — all pending). `weighted-kappa.ts` unit-tested (9 tests incl. a hand-computed value).
      _Rubric loads via `RubricRepository`; harness runs end-to-end (FakeAiGrader plumbing mode — 6 cases, per-criterion agreement table, injection check). `weighted-kappa.spec.ts` 9/9; `tsc` + `eslint` clean. **The kappa ≥ 0.6 SME run itself is `[B]`** — needs `ANTHROPIC_API_KEY` + the ~2-wk SME calibration pass (§13.1); until then every score from this rubric stays provisional + 100% human-reviewed, which the milestone state machine already honours via the `provisional` flag._

---

## Stage 2 — Account Lifecycle & Credential Brokering (after 0.6 gate)

**Stage status: NOT CLOSED — code complete + unit-tested, real-AWS runs `[B]` (0.6 gate unsigned, no Platform AWS account).** All 5 packages build (`go build/vet ./...` clean), full orchestrator suite green (28 packages), each package tested against real Postgres (testcontainers) + a `cloudaws.FakeClient`. The real path is wired in `cmd/orchestrator` behind `CLOUD_ACCOUNTS_ENABLED=true` + `AWS_*`/`PLATFORM_*` env — deploying is env config, not a code change. `cloudaws.RealClient` does the STS exchange with the aws-sdk-go-v2 `sts` client and shells out to `aws` / `aws-nuke` / `tofu` via `infra/nuke`, `infra/budget`, `infra/cost` wrapper scripts.

- [x] **2.1** OIDC + STS broker — `orchestrator/internal/credbroker/`: `Broker` (initial blocking mint → refresh loop at `RefreshFraction` of `CredTTL`, default 50%/1h; `Stop()` halts the loop so outstanding creds simply expire) + `Registry` (one broker per active T3 attempt; `StopFor(attemptID)` is what `cmd/orchestrator` wires to the attempt-left-`IN_PROGRESS` signal — the same stream the reaper consumes). `AssumeRoleWithWebIdentity` via `cloudaws.Client`; `CredsWriter` + `TokenSource` injected (real = emptyDir file + platform-IdP client).
      _`credbroker_test.go` 6 tests: initial mint + write, fail-fast on IdP error, the refresh loop writes ≥2× before expiry, `Stop()` halts all further refreshes, `Stop()` idempotent, `Registry.StopFor` stops + removes. `go test` + `go vet` + `gofmt` clean._
- [x] **2.2** Nuke + mandatory verification — `orchestrator/internal/cloudnuke/` + `infra/nuke/`: `Sweeper` (nightly re-nuke of every `AVAILABLE` + `QUARANTINED` account; a non-empty `AVAILABLE` account ⇒ `QUARANTINED` + page — the pool-leak case the §2 gate exists for; a `QUARANTINED` account that now verifies clean is **left** quarantined for a human). The release-path nuke is in `accountpool.Release` (calls `cloudaws.RunNuke`). `infra/nuke/run.sh` (assume `PlatformNukeRole` → render `aws-nuke.yaml.tmpl` → `aws-nuke run` → verification pass: Config `select-resource-config` + Resource Explorer `search` + a hardcoded blind-spot list = Route53 zones, leftover Budgets → `{verified, resources_remaining, blind_spot_hits, detail}` JSON). `docs/quarantine-runbook.md` (4.7).
      _`cloudnuke_test.go` 4 tests: clean `AVAILABLE` stays, leak ⇒ `QUARANTINED` + page, nuke-error ⇒ `QUARANTINED` + page, clean re-verify of a `QUARANTINED` account is **not** auto-released. `bash -n` clean on the scripts._
- [x] **2.3** Budget enforcement chain — `orchestrator/internal/cloudbudget/` + `infra/budget/`: `Enforcer.HandleBreach` (< 100% = emit a warning event; ≥ 100% = revoke the attempt's STS broker + force-terminate the T3 env, once per account, `ResetActed` on release) + `LaunchCap.Allow` (0 = uncapped; else `count(*) WHERE state='IN_USE' >= max` ⇒ `Provision` returns `ResourceExhausted`). `cmd/orchestrator` mounts `POST /cloud/budget-breach` on the metrics server (the SNS target; parses a raw or SNS-enveloped `{account_id, percent, amount_usd, budget_usd}`). `infra/budget/{put,delete}.sh` create/remove the per-account AWS Budget + SNS notifications at the thresholds.
      _`cloudbudget_test.go` 5 tests: warn thresholds don't revoke/terminate; ≥100% revokes + terminates; hard-stop is idempotent across a duplicate delivery + a 120% alarm, then acts again after `ResetActed`; launch cap allows under / rejects at the cap; `max=0` uncapped._
- [x] **2.4** Account Pool Manager — `orchestrator/internal/accountpool/`: `AVAILABLE → IN_USE → NUKING → (AVAILABLE | QUARANTINED)` backed by `env.cloud_account` (0.11) + a Redis `SPop` CAS fast path (so two concurrent T3 `Provision` calls never get the same account). `Claim` = SPop → guarded `UPDATE ... WHERE state='AVAILABLE'` → `PutAccountBudget` (2.3) → `ApplyBaseline` (1.3) → `SetSkuExceptionTag` (1.2) → emit `ACCOUNT_CLAIMED`; a partial-claim failure rolls the account to `NUKING` + async `Release`. `Release` = `NUKING` → `DeleteAccountBudget` → `RunNuke` (2.2) → clean ⇒ `AVAILABLE` + `ACCOUNT_NUKED`, else ⇒ `QUARANTINED` + `ACCOUNT_QUARANTINED` (`verification_nonempty` / `nuke_error`); idempotent. `Filler` re-syncs Redis + re-drives accounts stuck in `NUKING` > 10 min (crash recovery). New exported `RegisterAvailableAccount` / `StateOf` for ops + tests. `natsAccountEvents` in `cmd/orchestrator` publishes the 3 events on `env.telemetry.ACCOUNT_*` with the envelope shape.
      _`accountpool_test.go` 8 tests vs real Postgres + miniredis + `cloudaws.FakeClient`: happy claim → `IN_USE` + `ACCOUNT_CLAIMED` + the 3 AWS calls in order; empty pool ⇒ `ErrNoAccountAvailable`; **8-way concurrent claim yields exactly 1 winner**; clean release → `AVAILABLE` + re-claimable + `ACCOUNT_NUKED verified=true`; verification-nonempty ⇒ `QUARANTINED` + never claimable + `ACCOUNT_QUARANTINED`; nuke-error ⇒ `QUARANTINED`; `Release` idempotent; `Filler` re-drives a stuck-`NUKING` account._
- [x] **2.5** Independent Cost Explorer / CUR poll — `orchestrator/internal/cloudcost/` + `infra/cost/`: `Poller` — hourly `GetCostSince` per `IN_USE` (or recently-released) account grouped by the `attempt_id` tag → upsert `env.usage_meter` rows with `cloud_cost_usd` (keyed `(environment_id='cloud:<acct>', attempt_id, window_start)`, transactional UPDATE-then-INSERT since Phase-1 `usage_meter` has no unique constraint on that tuple); daily `ReconcileFromCUR` overwrites the CE estimate with the authoritative figure in place. `infra/cost/ce.sh` (Cost Explorer `get-cost-and-usage`) + `cur.sh` (Athena over the CUR table).
      _`cloudcost_test.go` 4 tests vs real Postgres: `pollOnce` writes a `cloud_cost_usd` row for an `IN_USE` account; **re-polling the same window does not duplicate**; the daily CUR reconciliation overwrites the CE estimate in place (1.00 → 1.37); an `AVAILABLE` account (no attempt) is skipped — `GetCostSince` never called._

---

## Stage 3 — T3 Driver, Editor & Full Evaluation (after 0.6 gate)

**Stage status: NOT CLOSED — code complete + unit/integration-tested; the `server.go` RPC wiring + real-T3 runs are `[B]` (0.6 gate, no multi-node cluster / AWS account).** The T3 driver, Snapshot/Restore, the 3 remaining validator executors, the viva, and `sp.project.default` are all built and tested; what remains is replacing the `Unimplemented` stubs in `internal/orchestrator/server.go` with calls into `t3driver` / `snapshotstate` (a change to the Phase-1 critical path, deliberately deferred behind the gate) and running it all against real cloud.

- [x] **3.1** OpenVSCode Server image — `infra/images/openvscode/Dockerfile` + `README.md`: `gitpod/openvscode-server` base + `aws` v2 + `tofu` (aliased `terraform`) + `kubectl` + `helm` + Terraform & Python language servers; binds `127.0.0.1:3000` only (no routable address into the pod, line 1040); reads AWS creds from `/var/run/secrets/aws/{credentials,config}` — the emptyDir the STS broker sidecar (2.1) refreshes.
      _Dockerfile authored; **not built/pushed** (needs a registry + multi-arch build). The 3.2 driver references it by `T3_EDITOR_IMAGE`._
- [x] **3.2** T3 driver — `orchestrator/internal/t3driver/`: `Driver.Provision` = `accountpool.Claim` (2.4) → `credbroker.Registry.Add` (2.1, blocking initial mint) → `PodManager.StartWorkspacePod` (editor + broker sidecar, no gVisor; a `FakePodManager` here, `internal/k8s` in production); a pod-start failure rolls back the broker + releases the account. `Connect` = editor + terminal WS URLs through the platform WS gateway with a scoped session token (`SessionTokenMinter.Register`, the Phase-1 mechanism) — no routable address. `Destroy` = `broker.StopFor` → `accountpool.Release` (nuke + verify) → `pods.DeleteWorkspacePod`. Wired in `cmd/orchestrator` via `CloudLifecycle`; `PROJECT_ORCHESTRATOR_GRPC=on` on practice-core points `GrpcProjectOrchestrator` at it.
      _`t3driver_test.go` 4 tests vs real Postgres + miniredis + fakes: Provision claims the account + starts the broker + pod + returns a ~1h creds expiry, with the claim-path AWS calls made; a pod failure leaves the account **not** `IN_USE` and stops the broker; Connect returns `wss://` URLs carrying the session token; Destroy stops the broker + releases the account to `AVAILABLE` + deletes the pod. **Wiring `server.go`'s `Provision`/`Connect`/`Destroy` for `TIER_T3_CLOUD_ACCOUNT` is `[B]`** (touches the Phase-1 path — gated on 0.6)._
- [x] **3.3** `Snapshot` / `Restore` IaC-state — `orchestrator/internal/snapshotstate/`: `Snapshot` = `terraform state pull` → serial, filtered `kubectl get all -A -o json`, `aws resource-explorer-2 search` tag-scoped to `attempt_id` → three blobs in S3 (`manifest.json` + the 2 inventories), manifest shape mirrors `pb.SnapshotManifest` (0.7); `DestroyCompute` (project idle-suspend default) drops the workspace pod. `Restore` = read manifest → `AccountReclaimer.ReclaimForAttempt` (prefer the original account) → re-provision the pod → `terraform init -reconfigure` + `apply` from the persisted backend state. All I/O behind `BlobStore` / `ShellRunner` / `PodLifecycle` / `AccountReclaimer` interfaces.
      _`snapshotstate_test.go` 5 tests vs fakes: Snapshot writes the manifest (serial 42, 3 cloud resources, account id) + 2 inventories + deletes the pod when asked; keeps the pod when not; a shell failure is an error; **Restore reads the manifest, prefers the original account, re-provisions the pod, and runs `terraform apply`**; a missing manifest is an error. **Replacing the `Unimplemented` stubs in `server.go` is `[B]`** (0.6 gate)._
- [x] **3.4** Swap the fake orchestrator for the real driver — `practice-core/src/modules/project/grpc-project-orchestrator.ts`: `GrpcProjectOrchestrator` implements `ProjectOrchestratorPort` over `contracts/orchestrator.proto` (`Provision` tier=`TIER_T3_CLOUD_ACCOUNT` / `Snapshot` / `Restore` / `Destroy`). Selected by `PROJECT_ORCHESTRATOR_GRPC=on` (a `project.module` factory toggle — the "swap Dev B's fake for the real driver" is one env var, no code change). The milestone state machine (1.6) already calls `provisionForMilestone` / `snapshotAndSuspend` per the env lifecycle rules (`design` = no env; pass ⇒ suspend; fail ⇒ provision-to-iterate).
      _`tsc` clean; the milestone integration spec builds `ProjectModule` with `GrpcProjectOrchestrator` overridden (default is still the fake). **A live run against the real orchestrator's T3 driver is `[B]`** (needs 3.2's `server.go` wiring + a cluster)._
- [x] **3.5** `MintValidatorCredentials` for cloud — the validator-scoped read-only STS path: `cloud-assert.executor.ts` (1.8) runs its `aws` CLI checks through the `ShellRunner`, which in production is `OrchestratorShellRunner` → `ExecShell` inside the T3 workspace pod, where the STS broker (2.1) has already brokered creds. A **validator-scoped** read-only role is minted the same way `credbroker` mints the learner role, but with a read-only policy — the orchestrator side of that is a small extension to `credbroker.Config` (a `RoleName` override) already supported; the read-only `LearnerSandboxValidatorRole` is defined in `infra/account-baseline/main.tf`'s pattern (add alongside `LearnerSandboxRole`).
      _The client dispatch + ERROR-not-FAIL handling is tested (14 t3-validator integration tests, 1.8). **The dedicated read-only role + its live PASS/FAIL run is `[B]`** (real AWS)._
- [x] **3.6** Chaos-action execution path — `chaos-probe.executor.ts` (3.7): D-P3-3's "reuse the pod-kill / node-drain mechanism" is implemented by running `kubectl` for the chaos action (`kill_pod` / `drain_node` / `cordon_node` / `delete_deployment_replica` / `sever_dependency` via a deny-all NetworkPolicy) through the workspace's own kubeconfig via the `ShellRunner` — no new orchestrator RPC needed (the thin `ExecChaosAction` is only warranted if a chaos action must target the learner's *deployed* system in the sandbox account, which `kubectl`-in-workspace covers).
      _Covered by the `runChaosProbe` unit tests (below, 3.7)._
- [x] **3.7** Remaining validator executors — `practice-core/src/modules/evaluation/t3/`: `runTestSuite` (JUnit XML / TAP / jest-json / go-json parsing → pass-rate gate, falls back to the exit code; `min_pass_rate`), `runPerfBench` (k6 run — a repo script or a generated constant-arrival-rate script → parse the summary export → `p95_ms_max` + `error_rate_max` gates), `runChaosProbe` (pre-check green → apply the chaos action → poll the health probe through the recovery window → PASS iff ≤1 failed probe **and** green at the end). All three dispatch from `T3ValidatorExecutor`; every platform failure → `ERROR`, never `FAIL`.
      _`test-suite.executor.spec.ts` (7) + `perf-chaos.executor.spec.ts` (9): no-config ⇒ ERROR, binary-missing ⇒ ERROR-not-FAIL, JUnit/TAP/jest-json parsed, sub-`min_pass_rate` ⇒ FAIL with counts; perf PASS/FAIL on p95; chaos ERROR when unhealthy pre-check, PASS through a pod kill, FAIL when the service doesn't recover, ERROR when the chaos action can't run. `tsc` + `eslint` + `lint:boundaries` clean; unit suite 210/210. **Real-T3-env runs are `[B]`.**_
- [x] **3.8** Defence viva — `practice-core/src/modules/project/`: `viva-model.port.ts` (`RealVivaModel` — direct Anthropic forced-tool-use call, D-P3-4, NOT the Mentor Service; `FakeVivaModel` — deterministic grounded questions from the design-doc headers + real commit shas) + `DefenceService` upgraded from a stub: `startViva` assembles the grounding facts from the learner's own repo (`GitService.readFile('DESIGN.md')` + `listCommits` + `diff`), generates 6–8 questions, emits the first as an EXAMINER `DEFENCE_MESSAGE` turn; `postMessage` replies turn-by-turn and on the last learner turn closes + runs `scoreViva` (renders the transcript, grades against `rub.reasoning.v1` via the shared `AI_GRADER`, emits a `DEFENCE_MESSAGE` score marker with `human_review_required` since the rubric is uncalibrated + certification-relevant). `content/rubrics/rub.reasoning.v1.yaml` — 4 criteria (`design_implementation_consistency`, `causal_reasoning`, `intellectual_honesty`, `overall`), `ALWAYS_PROVISIONAL_UNTIL_CALIBRATED`. Adversarial defence via `ClaudeAiGrader`'s existing delimit + flag (rule 35). `defenceComponentScore` maps the overall level 1–4 → [0,1] for 3.9.
      _`viva-model.port.spec.ts` (3): every question cites a real commit sha or design section, commits-then-sections-then-fallback priority, empty repo doesn't crash. `project-milestones.integration.spec.ts` +2 tests: the viva rejects before `final`, runs turn-by-turn, closes + scores with `human_review_required=true`, and emits the score-marker event. **The `rub.reasoning.v1` kappa calibration run is `[B]`** (`ANTHROPIC_API_KEY` + SME time)._
- [x] **3.9** `sp.project.default` scoring profile — `practice-core/src/modules/project/project-scoring.ts`: `ProjectScoringService.rollup` — milestone-weighted (design .2 / infra .15 / implementation .2 / hardening .2 / final .1 / defence .15). R5 (AI ≤ 40% of the total) is a **structural weight cap**: the AI-derived components (design + hardening rubric grades + the defence viva) are renormalised to sum to exactly 40% of the scoring weight, deterministic (infra + implementation + final validators) to 60% — so the model never determines > 40% of the outcome, but an all-perfect project still scores 1.0 (the cap reweights, it doesn't discount). Missing components contribute 0 at full weight. Per-skill mastery evidence = the final score apportioned by `activity_skill.weight` (§2.7). `ProjectService.finalizeProjectScore` runs the roll-up when `final` gates and persists `attempt_score` under `sp.project.default` + emits `EVALUATED` with the AI fraction + mastery evidence.
      _`project-scoring.spec.ts` (8): all-perfect ⇒ 1.0 with `aiFraction≈0.4`; deterministic-half-perfect / AI-half-zero ⇒ 0.6 (AI can't rescue); AI-half-perfect / deterministic-half-zero ⇒ 0.4 (capped); not-reached milestones drop weight; realistic mixed scores land between; mastery evidence weighted by skill weight; breakdown weights sum to 1 and contributions sum to `finalScore`; AI weight never > 40%. `project-milestones.integration.spec.ts` +1: finalizing `final` writes `attempt_score` under `sp.project.default` with `ai_fraction ≤ 0.41` + emits `EVALUATED`. Unit suite 210/210._

---

## Stage 4 — Analytics, Dashboard & Hardening

**Stage status: NOT CLOSED — backend (4.1/4.2/4.3) code-complete + integration-tested against real ClickHouse; `web/` UI (4.4–4.6), the T3-scale soak (4.8), and chaos-day (4.9) are `[B]` (no frontend in scope / no multi-node cluster + AWS).**

- [x] **4.1** ClickHouse ingestion — `practice-core/src/modules/analytics/`: `ClickHouseClient` (HTTP interface — `query` via JSONEachRow, `insert`, `command`, `ping`; enabled iff `CLICKHOUSE_URL` set) + `AnalyticsIngestionService` (a poll-based tail of `attempt.attempt_events` by `id` with a durable `analytics.ingestion_cursor` — the codebase has no NATS subject for the whole stream, so this is the "one source, many readers" fan-out shape; swapping to a NATS consumer later is a localised `fetchBatch` change). Each row is enriched with tenant / activity / course / user from `attempt` and bulk-inserted into `practice_analytics.attempt_events`; the hourly rollups attempt → activity → course → tenant → global are maintained by the ClickHouse MV (`infra/clickhouse/compose/init/01-schema.sql`). No-ops when `CLICKHOUSE_URL` is unset.
      _`analytics-pipeline.integration.spec.ts` vs **real ClickHouse** (compose profile) + real Postgres: `tick()` ships 20 seeded events → the MV rollup aggregates → the query layer returns them; **re-running `tick()` does not double-ingest** (cursor idempotency). `tsc` + `eslint` + `lint:boundaries` clean._
- [x] **4.2** Analytics query migration — `AnalyticsQueryService.eventRollup(tenant, grain, hours)` serves the rollups from **ClickHouse when `CLICKHOUSE_URL` is set, Postgres otherwise** — a runtime toggle, so a deployment without ClickHouse still works and Postgres stays the OLTP source of truth. `store()` reports which backend answered. Exposed at `GET /v1/admin/analytics/rollup?grain=…&hours=…`.
      _The "verify identical numbers" check is a **test**: `analytics-pipeline.integration.spec.ts` runs the same rollup against ClickHouse and Postgres and asserts `deepEqual` (20 events, 5 `MILESTONE_GATED`), plus a Postgres-only fallback test (always runs). **The past-10M-events/day load test is `[B]`** (needs volume + a real cluster)._
- [x] **4.3** Admin cost dashboard API — `practice-core/src/modules/admin/cost-dashboard.service.ts` + `admin.controller.ts`: `GET /v1/admin/cost/by-grain?grain=learner|course|activity|tenant` (blended compute + `cloud_cost_usd` + AI cost from `env.usage_meter`, joined to `attempt`), `GET /v1/admin/cost/accounts` (per-account spend + `quarantined` flags + budget from `env.cloud_account`), `GET /v1/admin/cost/budget-breaches` (from the `ACCOUNT_QUARANTINED` / `ACCOUNT_NUKED` event stream). All raw `sql` (env schema is Dev A's, not in the Kysely type); degrades to `[]` when the `env` schema isn't present (`hasEnvSchema()`).
      _`tsc` + `eslint` clean; wired into `AdminModule`. **Live numbers need real `env.usage_meter` / `env.cloud_account` data from a running T3 flow — `[B]`.**_
- [B] **4.4** Cost dashboard UI (`web/`) — cost-per-attempt distribution, cloud-vs-compute split, per-account spend with quarantine flags, budget-breach history.
      _Blocked: no `web/` frontend work in this session's scope. The 4.3 API is the contract it renders._
- [B] **4.5** T3 workspace chrome (`web/`) — OpenVSCode iframe surface, cloud-cred status indicator, budget banner. _Blocked: same as 4.4; also needs 3.2's `Connect` wired._
- [B] **4.6** Milestone tracker UI + viva chat surface (`web/`). _Blocked: same as 4.4. The `GET .../milestones` + `POST .../defence/messages` APIs (1.6/3.8) are the contract._
- [x] **4.7** Sweeper hardening + quarantine runbook + capacity guidance — `orchestrator/internal/cloudnuke.Sweeper` (a leak in an `AVAILABLE` account ⇒ `QUARANTINED` + page + a `SWEEPER FOUND A LEAK` error log; a `QUARANTINED` account that re-verifies clean is **never** auto-released); `orchestrator/docs/quarantine-runbook.md` (per-`reason` investigation + remediation + the "sweeper quarantine = pool leak = §2 regression" call-out + the capacity note); launch-cap tuning is `T3_LAUNCH_CAP` (2.3); `account_pool_depth` gauge + the filler's below-target warning cover quota headroom.
      _Runbook written; sweeper behaviour covered by `cloudnuke_test.go`. **Live headroom review + cap tuning against a real cohort is operational, `[B]`.**_
- [B] **4.8** T3-scale teardown load-soak — zero orphan environments AND zero orphan cloud resources sustained through the run + 24 h. _Blocked: needs a real multi-node cluster + AWS accounts. `evaluation/phase1/soak/namespace-churn-soak.sh` (0.3) is the T1/T2 harness; the T3 extension reuses `accountpool` + `cloudnuke` under load._
- [B] **4.9** Chaos-day (joint) — kill accounts mid-nuke and mid-provision; confirm zero orphans + correct `QUARANTINED` routing. _Blocked: needs real AWS. The routing logic is unit-tested (`accountpool` rollback-to-`NUKING`, `cloudnuke` quarantine branches); chaos-day proves it under real failure injection._
- [ ] **4.10** Closure cross-check — `go build/vet/test ./...` clean in `orchestrator/` ✅ (28 packages, incl. the 7 new Phase-3 packages); `npm test` clean in `practice-core/` ✅ (210/210 unit; 15/16 integration suites — the 1 failure, `attempt-lifecycle` cooldown, **fails on the untouched baseline** and is unrelated); `web/` not in scope; compose-backed integration green ✅ (Forgejo + ClickHouse deployed locally + verified). **`no stub/fake/mock on any T3 critical path` is NOT yet true** — `server.go` still returns `Unimplemented` for `Snapshot`/`Restore` and has no T3 `Provision` branch (deferred behind 0.6); `FakeProjectOrchestrator` / `FakeVivaModel` / `cloudaws.FakeClient` are the defaults until the real env vars are set. **Reviewer sign-off: pending. This stage cannot be `[x]` until 0.6 is signed, the `server.go` wiring lands, and the T3-scale soak passes on real infra.*